import axios from "axios";
import { PATIENT_LOGIN } from "./types";

export const patientLogin = userData => dispatch => {
  console.log(userData);
};
export const patientRegister = userData => dispatch => {
  console.log(userData);
};

export const physicianLogin = userData => dispatch => {
  console.log(userData);
};

export const adminLogin = userData => dispatch => {
  console.log(userData);
};
